[tool.ruff]
# Длина строки
line-length = 88
# Исключаем
exclude = [".venv", "__pycache__", "build", "dist"]

[tool.ruff.lint]
# Проверка
select = ["E", "W", "F", "I", "N", "UP", "B"]
# Игнор
ignore = ["E501", "D100", "D104"]
